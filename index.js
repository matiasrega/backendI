import fs from "fs";
import http from "http";

/*const server = http.createServer((req, resp) => {
  resp.end("servidor de alta");
});

server.listen(8080, () => {
  console.log("Servidor dado de alta");
});*/
